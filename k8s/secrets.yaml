apiVersion: v1
kind: Secret
metadata:
  name: nexora-secrets
  namespace: nexora
  labels:
    app.kubernetes.io/part-of: nexora
type: Opaque
stringData:
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
---
# Template for per-service secrets
# Copy this and replace with actual values before applying
# Example for identity-service:
# apiVersion: v1
# kind: Secret
# metadata:
#   name: identity-service-secrets
#   namespace: nexora
# type: Opaque
# stringData:
#   JWT_SECRET: "your-256-bit-secret-here"
#   MFA_ENCRYPTION_KEY: "your-mfa-key-here"
